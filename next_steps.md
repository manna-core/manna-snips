# Manna Snips Next Steps

1. Make the first low-noise public post.
   Status: the README, repo metadata, screenshots, and `docs/launch-plan.md` are now strong enough for strangers to land on the project without confusion.
   Next step: use the `r/sideprojects` draft in `docs/launch-plan.md`, keep it problem-first, and stay present in the thread after posting.

2. Follow with discovery surfaces that compound over time.
   Status: the app is already publicly installable, but it is still missing slower-burn discovery channels.
   Next step: submit `Manna Snips` to `winget` and add it to AlternativeTo.

3. Keep the safer shipping recommendation honest.
   Status: the bundled-runtime installer is the preferred public lane, and the portable PyInstaller zip is available as secondary.
   Next step: keep recommending the installer publicly unless the older portable-build trust question is explicitly revisited and cleared.

4. Keep the public surface simple.
   Status: the app and README are already in the subtractive `Crystal Utility` lane, and the hotkey picker landed without creating a separate settings maze.
   Next step: if more UI polish happens, prefer clearer defaults and fewer words over extra chrome or explanatory panels.

5. Continue post-release trust work.
   Next step: clean-machine validation and deeper packaged-launch confidence are still the highest-value follow-ups after `v0.1.1` and before any bigger feature expansion.

6. Reuse the written retrospective.
   Status: `docs/retrospective-v0.1.1.md` now captures the real wins, misses, and disturbance-minimizing habits from the first shipping cycle.
   Next step: use it as the baseline release template for future Manna utilities instead of re-learning source/release/install/test-copy hygiene from scratch.
